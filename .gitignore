# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Local env files
.env
.env.local
.env.*.local

# Editor & OS
.DS_Store
.vscode/
*.local
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local scripts & private docs — not pushed to git
deploy.ps1
Project-update.md
private-docs/
*.local-backup
src/App.dev.tsx

# Project update notes (local only)
project_updates/

# Test Cup — local engine validation only, never pushed/deployed
# Only the testCup data is excluded — all other dynamic engine files
# are shared infrastructure needed by WC26 and must be in git.
src/data/competitions/testCup/